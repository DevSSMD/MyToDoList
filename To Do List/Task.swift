


import Foundation

struct Task {
    var title: String
    var isComplete: Bool
    
    init(title: String, isComplete: Bool) {
        self.title = title
        self.isComplete = isComplete
    }
}
